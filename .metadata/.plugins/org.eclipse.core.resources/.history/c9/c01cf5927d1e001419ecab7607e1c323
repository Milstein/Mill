package nineMensMorris;

public class NineGame extends Game {
	//0 for none, 1 for player 1, 2 for player 2, 3 for selected player 1, 4 for selected player 2
	private int[] slots;
	//0 for placing, 1 for moving, 2 for flying
	private int phase = 0;
	
	public NineGame() {
		setDisplay(new NineGameDisplay(this));
		slots = new int[24];
	}
	
	protected int getSlot(int index) throws Exception {
		if (!(0<=index && index<=23))
			throw new Exception("Accessed outside of slot bounds");
		return slots[index];
	}
	
	public boolean clickPosition(int square, int location) {
		switch
	}
}
